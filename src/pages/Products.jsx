// import { useLoaderData } from "react-router-dom";
// import Product from "./Product";

const Products = () => {
  // const data = useLoaderData();
  return (
    <div>
      <h2> Hi </h2>
      {/* <h2> {data.length} </h2>
      {
        data.map((product, idx) => <Product key={idx} product={product}></Product>)
      } */}
    </div>
  );
};

export default Products;